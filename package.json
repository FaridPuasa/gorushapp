{
  "name": "gorushapp",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm install --prefix gorush-server && npm install --prefix gorush-client && npx --prefix gorush-client expo export -p web --output-dir ../gorush-server/webapp",
    "start": "npm start --prefix gorush-server"
  },
  "engines": {
    "node": ">=20"
  }
}
