{
  "name": "gorushapp",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd gorush-server && npm install && cd ../gorush-client && npm install && npx expo export -p web --output-dir ../gorush-server/webapp",
    "start": "cd gorush-server && npm start"
  },
  "engines": {
    "node": "24.x"
  }
}
